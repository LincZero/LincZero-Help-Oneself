cat /etc/issue
sudo apt update

# node22

# python3、pip

# 下载一些工具
# (现在越来越多的 Linux 命令行工具选择通过 npm 或 pip3 分发，而不是传统的 apt 包管理器)
# (方便快速迭代，跨平台，减少严格审核)
pip3 install thefuck
npm install -g tldr # or pip3 install tldr

# 下载Clash Verge
# 手动下载Clash Verge的deb文件
sudo apt install Cl<tab>

# 下载QQ
# 手动下载QQ的deb文件
sudo apt install QQ<tab>
